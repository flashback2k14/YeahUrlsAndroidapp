<resources>
    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!--
       Base application theme, dependent on API level. This theme is replaced
       by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Base.Theme.YeahURLs">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <style name="Base.Theme.YeahURLs" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <!-- darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- theme UI controls like checkboxes and text fields e.g. FloatActionButton -->
        <item name="colorAccent">@color/accent</item>
        <!-- Title Text Color -->
        <item name="android:textColorPrimary">@color/window_foreground</item>
        <item name="actionOverflowMenuStyle">@style/OptionsMenu</item>
        <item name="android:windowBackground">@color/window_background</item>
        <!-- Drawer Style -->
        <item name="drawerArrowStyle">@style/AppTheme.MyDrawerArrowStyle</item>
        <!-- Definition Text Hint -->
        <item name="android:textColorHint">@color/secondary_text</item>
    </style>

    <!-- Definition Option Menu Style -->
    <style name="OptionsMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <!-- color of the menu overflow icon (three vertical dots) -->
        <item name="android:textColorSecondary">@color/window_foreground</item>
        <item name="android:popupBackground">@color/window_background</item>
        <item name="android:textColor">@color/window_foreground</item>
    </style>

    <!-- Definition Drawer Style -->
    <style name="AppTheme.MyDrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/window_foreground</item>
    </style>

    <!-- Definition Edittext Style -->
    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/secondary_text</item>
        <item name="colorControlActivated">@color/accent</item>
        <item name="colorControlHighlight">@color/accent</item>
        <item name="android:textColor">@color/secondary_text</item>
    </style>

    <!-- Definition Dialog Style -->
    <style name="ThemeCustomDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorButtonNormal">@color/accent</item>
    </style>

</resources>
